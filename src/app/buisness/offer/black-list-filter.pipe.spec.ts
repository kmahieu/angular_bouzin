import { BlackListFilterPipe } from './black-list-filter.pipe';

describe('BlackListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BlackListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
